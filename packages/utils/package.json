{
    "name": "@petk/utils",
    "version": "0.0.0",
    "main": "src/index.ts",
    "license": "MIT",
    "scripts": {
        "dev": "tsx --watch src/index.ts",
        "build": "swc src -d dist --source-maps",
        "test": "vitest run",
        "lint": "eslint ."
    }
}