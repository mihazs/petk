{
    "name": "@petk/engine",
    "version": "0.0.0",
    "main": "src/index.ts",
    "license": "MIT",
    "scripts": {
        "dev": "tsx --watch src/index.ts",
        "build": "swc src -d dist --source-maps",
        "test": "vitest run",
        "lint": "eslint ."
    },
    "devDependencies": {
        "@swc/cli": "^0.7.8",
        "@swc/core": "^1.13.3",
        "vitest": "^3.2.4"
    },
    "dependencies": {
        "@petk/utils": "workspace:*"
    }
}